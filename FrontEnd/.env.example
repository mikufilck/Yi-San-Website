# =================================================================
# 一三设计 - 前端配置模板
# 使用方法: 复制此文件并重命名为 .env (开发) 或 .env.production (生产)
# =================================================================

# --- API 接口地址 ---
# 开发环境通常是 http://localhost:8000/api
# 生产环境通常是 https://api.yourdomain.com/api 或直接 /api
VITE_API_BASE_URL=http://localhost:8000/api

# --- 静态资源根地址 (用于补全图片路径) ---
# 开发环境通常是 http://localhost:8000
# 生产环境如果是 Nginx 转发，可留空或设为 https://api.yourdomain.com
VITE_ASSET_BASE_URL=http://localhost:8000

# --- 其他配置 ---
VITE_APP_TITLE="一三设计"
